// .env.example

# Set to "true" (case-sensitive) to run the respective part.
RUN_EXTRACT=true
RUN_FOLLOW=true

# Main account credentials for extraction
MAIN_USERNAME=your_main_username
MAIN_PASSWORD=your_main_password

# Side account credentials for following
SIDE_USERNAME=your_side_username
SIDE_PASSWORD=your_side_password

# Comma-separated list of account names to skip (without spaces)
SKIP_NAMES=examplePage1,examplePage2
